export interface Course {
  id: string;
  titleFa: string;
  titleEn: string;
  descriptionFa: string;
  price: number;
  instructor: string;
  instructorImage: string;
  image: string;
  durationHours: number;
  active: boolean;
  createdAt: string;
}
